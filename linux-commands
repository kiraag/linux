ls				-		list all files & folder

mkdir <directory name>		-		create an empty dirctory

cd <directory name>         -       opens the directory

pwd                         -       shows present working dirctory

rm <file name>		-	delete file

rmdir <directory name>		-	deletes empty directory

rm -rf <directory name>		-	delete directory with files including

mv <file name> <path>       -   moves files to given path

mv <file old name> <file new name>      -       renames the files (same command used to move files also)

cp <file name> <destination path>       -       copy files to given destination path

